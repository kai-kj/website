# Structure

- `kaikitagawajones.com/` $\rightarrow$ `index()` \
  About me; link to posts; link to projects; link to photos; (link to art); links to GitHub, LinkedIn, email; 
  - `posts/` $\rightarrow$ `posts()`
    - `xxx/` $\rightarrow$ `post()`
  - `photos/`
  - `projects/`
  - (`art/`)
  - `files/`